services:
  app:
    image: ${IMAGE}
    container_name: ecr-ec2-app
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      PORT: 3000
      APP_VERSION: ${APP_VERSION:-1.0.0}
      GIT_SHA: ${GIT_SHA:-local}
